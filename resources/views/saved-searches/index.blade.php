@extends('themes.xylo.layouts.master')
@section('content')<div class="container py-4"><h1>Saved Searches</h1><div class="row g-3">@forelse($savedSearches as $search)<div class="col-md-6"><div class="card"><div class="card-body"><h5>{{ $search->name }}</h5><p class="mb-1">{{ $search->query ?: 'Filtered marketplace search' }}</p><a class="btn btn-sm btn-primary" href="{{ route('search.results', array_merge(['q'=>$search->query ?: 'all'], $search->filters ?: [])) }}">Run Search</a><form class="d-inline" method="POST" action="{{ route('saved-searches.destroy',$search) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></div></div>@empty<div class="alert alert-light">No saved searches yet.</div>@endforelse</div></div>@endsection
